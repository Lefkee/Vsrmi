[package]
name = "termi"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A modern, modular terminal code editor written in Rust"
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

# Normalise line endings so a contributor with core.autocrlf=true cannot turn
# an editor's worth of source into a whole-file diff.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary

# Keep generated or vendored files out of diffs and language statistics.
Cargo.lock linguist-generated=true -diff
